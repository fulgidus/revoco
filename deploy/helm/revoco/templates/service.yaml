apiVersion: v1
kind: Service
metadata:
  name: {{ include "revoco.fullname" . }}
  labels:
    {{- include "revoco.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "revoco.selectorLabels" . | nindent 4 }}
